import SwiftUI

/// App entry point (PR 0 skeleton). No navigation or domain screens yet.
@main
struct StudentOSApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
